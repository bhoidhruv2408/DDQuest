<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Your existing themes -->
    <style name="Base.Theme.DDQuest" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.DDQuest.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.DDQuest" parent="Base.Theme.DDQuest" />

    <!-- NEW: Splash Theme – Full black, fullscreen, compatible with AppCompat -->
    <style name="Theme.DDQuest.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">#000000</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
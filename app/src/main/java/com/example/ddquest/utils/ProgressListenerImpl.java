package com.example.ddquest.utils;

public class ProgressListenerImpl extends FirebaseStorageUtil.ProgressListener {
    public ProgressListenerImpl(FirebaseStorageUtil.ProgressCallback callback) {
        super(callback);
    }
}
